import videoDownloadHandler as downloader
import startingPage as home

if __name__ == "__main__":
    # path = str(input("Insersci la cartella di destinazione del file \n-->"))
    # url = str(input("Insersci il path del video da scaricare \n-->"))
    # downloader.downloadVideoByUrl(url, path)

    home.main_window()